# AGENTS.md

- 更新日期：2026-05-15
- 执行者：Codex
- 适用范围：仓库根目录及其全部子目录；若子目录存在更具体的 `AGENTS.md`，以子目录文件为准。

## 1. 项目定位

`easy-strm` 是一个轻量级媒体整理与 `.strm` 生成工具，采用 Go 后端 + Vue 3 前端，主链路为：

```text
媒体源接入 -> 文件浏览 -> TMDB 识别 -> 重命名/整理 -> STRM 生成 -> 任务追踪
```

仓库结构：

```text
easy-strm/
├── easy-strm/         # Go 后端，Gin + PostgreSQL + Redis
├── easy-strm-front/   # Vue 3 前端，Vite + Element Plus
├── deploy/            # Docker 部署文件
├── docker/            # 镜像与运行环境配置
├── docs/              # 长期维护文档
└── debug/             # 本地调试与 E2E 产物
```

## 2. 工作原则

1. 先理解现有实现，再修改代码；新增能力前至少查看相邻模块、相似 Service、相似前端组件。
2. 优先复用项目已有分层、工具函数、API 封装、样式体系和测试方式，不新增重复维护面。
3. 每次变更保持小步、可编译、可验证；不要提交半成品、占位实现或只覆盖 happy path 的代码。
4. 业务规则优先沉淀在后端 `service` 层；前端负责交互状态、展示编排和用户反馈，不承载核心整理规则。
5. 接口变更必须同步更新 `domain`、`dao`、`service`、`controller`、前端 API 封装、页面使用方和测试。
6. 删除或迁移旧实现时要确认没有被路由、任务、E2E 脚本、部署配置或文档引用。

## 3. 架构分层规范

### 3.1 后端分层

- `domain`：定义领域模型、请求结构、响应结构、枚举常量和纯数据规则。
- `dao`：封装 PostgreSQL / Redis 访问，不编排业务流程，不返回前端专用结构。
- `service`：承载业务规则、流程编排、外部系统交互、任务状态推进和错误归一化。
- `controller`：只做参数解析、校验入口、调用 service、HTTP 响应包装。
- `internal/pkg`：放置跨业务的基础能力，例如日志；新增公共包前必须确认至少两个以上模块复用。
- `migrations`：数据库结构变化必须通过迁移脚本表达，命名保持版本递增和语义清晰。

后端依赖方向：

```text
controller -> service -> dao -> database/cache
service -> domain
dao -> domain
```

禁止反向依赖：`dao` 不依赖 `service`，`domain` 不依赖 `controller` 或具体基础设施。

### 3.2 前端分层

- `src/views`：页面级容器，负责页面布局、路由入口和跨组件状态协调。
- `src/components`：可复用或领域内组件，避免直接散落全局副作用。
- `src/utils/api`：所有 HTTP 调用统一封装在这里，页面和组件不要直接拼接后端 URL。
- `src/utils/ui`：统一交互工具，例如二次确认、提示框、可复用 UI 行为。
- `src/style.css`：全局视觉基线和公共样式，组件私有样式优先留在组件内。

前端页面应优先拆分为“页面容器 + 业务组件 + API 模块”，避免单个 `.vue` 文件继续膨胀。

## 4. 设计原则

代码必须符合以下设计原则：

- 单一职责原则：函数、结构体、组件只承担一个清晰职责；超过一个业务阶段时优先拆分。
- 开闭原则：新增媒体源类型、整理策略、命名策略时优先扩展策略对象或接口实现，避免在既有大函数中无限追加分支。
- 里氏替换原则：接口实现必须保持一致语义，不用特殊实现破坏调用方预期。
- 接口隔离原则：接口按调用场景定义，避免把读、写、任务控制、外部客户端能力揉成一个胖接口。
- 依赖倒置原则：Service 依赖抽象接口或构造注入的协作者，外部系统调用要能在测试中替换。
- 最少知识原则：上层模块只了解必要协作者，不跨层读取数据库、Redis 或组件内部状态。
- DRY 但不过早抽象：重复出现三次以上且语义稳定时再抽公共能力。
- KISS：优先选择直观、可测试、易排错的实现，不为炫技牺牲可读性。

## 5. 设计模式使用规范

设计模式是为业务变化服务，不是为了形式完整。新增或重构时优先考虑以下模式：

- Repository / DAO：数据库和缓存访问统一放在 `dao`，Service 不直接写 SQL 或拼 Redis Key。
- Service Layer：业务用例统一由 `service` 表达，Controller 不承载跨步骤流程。
- Strategy：媒体源差异、本地与 115 操作差异、冲突策略、整理模式、分类匹配规则应优先以策略封装。
- Factory：创建外部客户端、策略实例、任务处理器时使用工厂函数集中构造，避免调用方散落初始化细节。
- Adapter：对 TMDB、Emby、115、文件系统等外部能力做适配，内部业务只依赖稳定接口。
- Template Method / Pipeline：识别、预览、整理、刮削、生成 STRM 等多阶段流程可抽成清晰步骤，便于插入验证和测试。
- Command：任务中心、异步任务、可取消动作适合用命令式对象表达输入、执行、状态和结果。
- Observer / Pub-Sub：目录监控、任务状态通知、日志事件只在确有多订阅方时使用，避免过度事件化。

禁止事项：

- 禁止为了套设计模式新增空接口、空结构体或只有一个实现且没有变化点的抽象。
- 禁止把所有逻辑塞进通用 Manager；命名必须体现真实职责。
- 禁止在前端组件中复制后端业务策略，前端只消费后端给出的规则结果。
- 禁止新增与现有 DAO / Service / API 封装平行的第二套访问体系。

## 6. Go 代码规范

### 6.1 基础风格

- 使用 `gofmt` / `go test` 保持格式和可编译状态。
- 包名使用简短小写单词，不使用下划线。
- 导出类型、函数和方法必须有中文注释，说明职责、关键约束和返回语义。
- 非导出函数只在逻辑不直观、涉及边界条件或跨系统语义时补充中文注释。
- 错误信息面向排查，日志中保留上下文，返回给前端的信息保持可读。
- 不吞错误；不能处理的错误必须向上返回或转为明确业务状态。

### 6.2 函数与结构体

- 函数长度优先控制在一个清晰屏幕内；超过多个阶段时拆出私有函数。
- Service 构造函数统一使用 `NewXxxService(...)`，依赖通过参数注入。
- Controller 构造函数统一使用 `NewXxxController(...)`，路由注册处只负责装配。
- 领域对象字段命名与 JSON 字段保持稳定，避免前端需要兼容多套字段。
- 归一化函数使用 `normalizeXxx` 命名，校验函数使用 `validateXxx` 命名，构建函数使用 `buildXxx` 命名。

### 6.3 数据与事务

- SQL 访问放在 DAO，查询字段显式列出，不依赖 `SELECT *`。
- 新增表或字段必须提供迁移脚本，默认值、索引、非空约束要与业务读取路径一致。
- 多步骤写入必须考虑部分失败；需要原子性的流程使用事务。
- Redis Key 命名必须有业务前缀和稳定维度，避免与历史 Key 冲突。

### 6.4 并发与任务

- goroutine 必须有退出条件；涉及任务取消时使用 context 或已有任务取消机制。
- 轮询、监控、自动整理必须有明确终态，避免无界循环和重复提交。
- 共享状态必须明确同步边界，不在多个 Service 中隐式修改同一状态。

## 7. Vue 代码规范

### 7.1 组件结构

- 优先使用 Vue 3 组合式 API；既有组件若使用当前风格，改动时保持局部一致。
- 页面组件负责组装，业务组件负责局部交互，API 模块负责请求。
- 复杂弹窗必须把打开、关闭、重置、轮询停止、加载状态释放写成明确流程。
- `ref`、`computed`、事件处理函数按“状态 -> 派生状态 -> 行为 -> 生命周期”的顺序组织。

### 7.2 交互与状态

- 不用一个全局 `loading` 锁死整个页面或弹窗；按按钮、列表、预览区拆分加载态。
- 弹窗关闭、刷新预览、切换媒体源、重新发起任务时必须停止旧轮询。
- 前端错误提示必须可读，优先展示后端返回的业务信息，再补充默认兜底文案。
- 批量操作必须在界面上呈现选中数量、目标路径、冲突策略和执行结果。

### 7.3 样式与 UI

- 复用 Element Plus 组件和项目既有样式变量，不新增平行 UI 框架。
- 新增页面应保持当前媒体工作台的视觉语言：清晰分区、明确主操作、可读的状态卡片。
- 样式命名使用语义化 class，避免只表达颜色或位置的命名。
- 移动端和窄屏场景至少保证主要流程可操作，不出现关键按钮不可见。

## 8. 接口契约

- 后端成功响应统一使用项目已有 `SuccessResp`，失败响应统一使用 `ErrorResp`。
- Controller 入参必须校验类型、必填字段、枚举值和路径边界。
- 前端 `src/utils/api` 中的函数名要表达业务动作，不暴露 HTTP 细节给页面。
- 新增接口必须在后端路由注册处、前端 API 封装、调用页面、测试中同步落地。
- 列表接口应返回稳定的 `data` 和 `total` 结构；详情接口返回稳定对象。
- 涉及任务的接口必须返回可追踪任务 ID，并能在任务中心查看状态。

## 9. 测试与验证

### 9.1 默认验证基线

后端：

```powershell
Set-Location .\easy-strm
go test ./...
```

前端：

```powershell
Set-Location .\easy-strm-front
npm run build
```

浏览器主流程按改动范围选择相关 E2E：

```powershell
Set-Location .\easy-strm-front
npm run e2e:organize-preview-refresh
npm run e2e:organize-preview-cancel
npm run e2e:resource-platform
```

### 9.2 测试编写要求

- Service 新增业务规则必须补单元测试。
- DAO 新增查询、写入、迁移相关逻辑必须补 sqlmock 或等价本地测试。
- Controller 新增接口必须覆盖参数错误、成功响应和关键失败分支。
- 前端核心流程变更至少通过 `npm run build`，必要时补浏览器 E2E 或脚本化验证。
- bug 修复必须先补能复现问题的测试，再修复实现。

### 9.3 高风险回归点

- 媒体源创建、编辑、启停与排序。
- 本地目录浏览、115 文件浏览、分页、搜索、视频过滤。
- TMDB 识别、手动修正、缓存回流。
- 重命名预览、整理预览、复制/移动/硬链接/软链接。
- STRM 生成路径、任务创建、任务取消、任务恢复。
- 自动整理、目录监控、失败分类、任务中心展示。

## 10. 文档规范

- 长期规范、测试基线、历史经验优先更新 `docs/开发与测试.md`。
- 产品行为、核心流程、架构说明优先更新 `docs/产品与架构.md`。
- 一次性调试证据放入 `debug/`，不要在 `docs/` 下制造时间戳报告碎片。
- 文档必须标注更新日期和维护者；涉及外部资料时记录来源 URL 或本地文件路径。
- 代码注释使用中文，说明意图、约束和使用方式，不写机械翻译式注释。

## 11. 依赖与配置

- 后端依赖通过 `go.mod` 管理；新增依赖前确认标准库或现有依赖无法满足。
- 前端依赖通过 `easy-strm-front/package.json` 管理；新增依赖必须服务明确业务场景。
- 不新增第二套 HTTP 客户端、路由系统、状态管理库或样式框架，除非已有方案无法满足且收益明确。
- `.env.example`、`deploy/`、`docker-compose.yml`、`Dockerfile` 变更必须同步检查本地启动文档。
- 不提交真实密钥、账号、Token、Cookie、数据库密码或个人路径。

## 12. 提交流程

1. 阅读相关代码、文档和相似实现。
2. 明确接口契约、设计模式选择、风险点和验证方式。
3. 小步实现，保持每步可编译。
4. 同步更新测试、文档和前端/后端调用方。
5. 执行本地验证命令，记录失败原因和修复结果。
6. 自查是否存在重复抽象、跨层访问、未停止轮询、未处理错误、未覆盖边界条件。

## 13. 质量门禁

合并前必须满足：

- 代码符合既有目录职责和命名风格。
- 关键业务逻辑位于 Service，Controller 和前端没有复制业务规则。
- 设计模式使用有明确变化点，不存在空抽象或过度工程。
- 新增接口有测试或明确的本地验证证据。
- 前端构建通过，后端测试通过；无法执行的验证必须说明原因和风险。
- 文档与实际行为一致，没有保留过时说明。

## 14. 常见反模式

- 在 Controller 中写复杂业务流程。
- 在 Vue 页面中直接拼后端 URL 或复制整理规则。
- 在一个 Service 方法里混合参数校验、路径扫描、外部请求、数据库写入、任务状态推进和响应拼装。
- 为一个实现创建没有收益的接口，或为一个分支创建复杂策略树。
- 新增字段只改后端不改前端，或只改页面不改 API 封装。
- 使用全局状态临时绕过依赖注入，导致测试难以替换。
- 异步任务没有取消、没有终态、没有错误归因。
- 修复 bug 后不补回归测试。
