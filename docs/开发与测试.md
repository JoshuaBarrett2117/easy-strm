# 开发与测试

## 播放记录季集回归（2026-09-13，维护者：Codex）

覆盖普通直链控制器→Service→DAO 在重复请求下仅持久化一条带季集标题的记录；分享导出保存多集快照，DAO 读取季集 JSON，标题格式化覆盖电影、普通剧集、第 0 季、多集合一、重复和无效季集。验证 `go test ./...`、`npm run build`，前端长标题换行保证季集可见。接口继续使用 `name`，无需数据库迁移。

## 分享资源库季集详情（2026-09-13，维护者：Codex）

电视剧详情使用 `/media/share-library/tv-detail` 加载完整季概览，展开季时再调用 `/media/share-library/tv-seasons`。后端测试覆盖完整 TMDB 目录与本地文件映射合并、特别篇、同集多来源、一文件多集、TMDB 失败降级及参数错误；不需要数据库迁移。前端 `e2e-library-schedules.mjs` 同时验证电影原来源列表、电视剧按季懒加载、无文件集、失效分享跳转和窄屏布局。

## STRM 播放会话去重（2026-09-13，维护者：Codex）

运行版本回归：前端 Vite 更新不代表 Go 后端已更新。此次重复记录复查发现，8082 实际运行的 `debug/easy-strm-library-fix.exe` 编译于 11:58，而去重代码修改于 12:11。`start.bat` 现先编译当前源码到临时 exe，成功后替换并启动 `debug/easy-strm-dev.exe`；`restart.bat` 复用此流程，编译失败保留原服务。验收需核对监听端口的进程路径及编译时间，不能仅凭前端提示判断修复已生效。

`TestDirectLinkRepeatedRequestsPersistOneRecord` 联动真实控制器、Service 与 sqlmock DAO，验证 12 次 HEAD/GET 返回不同 CDN 地址时只写入一条记录。历史记录不自动删除；当前去重状态保存在进程内，重启会重新建立会话。同一代理 IP 下的客户端无法仅凭 IP 区分，五分钟规则是解析会话近似值，并非精确观看次数。

普通与分享 STRM 的成功 GET/HEAD 解析仍会触发播放记录入口，但 Service 会按“资源稳定标识 + 来源 IP”合并连续调用；同一组合每次重复解析都会延长会话，静默超过 5 分钟后再次访问才新增记录。该规则用于吸收 Emby/Jellyfin 对 115 临时 CDN 地址的预解析、重试和刷新，不会把不同文件或不同来源合并。

后端回归测试覆盖同会话 HEAD/GET 合并、不同文件、不同来源、超时后重播、并发请求及首次保存失败后重试。前端播放记录抽屉同步说明展示的是播放会话，而不是原始直链请求数。

## STRM 输出保护（2026-09-12，Codex）

`go test ./...` 包含输出归属冲突、未登记文件保护、无变化不重写、丢失文件补回、目录越界及锁冲突回归测试。`node scripts/e2e-strm-options.mjs` 验证全量弹窗默认不清空及显式清空参数；可用 `E2E_BASE_URL` 指定测试前端。

迁移 v34 需在新版服务启动后验证，真实共享目录的删除与导出不能用 UI mock 测试代替。清空阶段按任务 ID 持久化；重入同一任务不会重复清空。新增 STRM 任务保存执行配置，任务中心恢复会重新派发执行；缺少配置的旧任务提示从配置页面重新生成。

## 分享资料库导出进度（2026-09-10，Codex）

导出弹窗复用TaskCard显示进度，每秒查询当前任务；关闭/卸载停止，重新打开继续查询，完成/失败/取消后停止，查询错误可重试。任务中心默认开启3秒列表刷新，详情沿用2秒刷新并显示进度条、百分比和处理数量。`node scripts/e2e-share-strm.mjs`覆盖25%→75%→完成、关闭与终态停止轮询、详情100%显示；`npm run build`通过。日志为`.codex/share-strm-progress-e2e.log`与`.codex/share-strm-progress-build.log`。

## 分享资料库 STRM 回归（2026-09-10，维护者：Codex）

- 后端在 `easy-strm` 执行 `go test ./...`。新增 `ShareStrm` 测试覆盖仅用本地记录导出（115客户端为空仍成功）、目录记录不补扫、首次播放定向定位及文件ID缓存、分类路径、季集与特别篇、季目录补全、手动修正、部分失败保留、稳定URL、异步任务取消、并发播放仅转存一次、重启复用、上游失败、UA传递、GET/HEAD 302、DAO分页/映射/事务锁。
- 前端在 `easy-strm-front` 执行 `node scripts/e2e-share-strm.mjs`，脚本自动启动 127.0.0.1:3107 的 Vite 并在结束时关闭；使用 Playwright 和本地模拟 API，覆盖配置持久化回显、已应用筛选全量导出、保存失败阻止任务创建、错误恢复和390px窄屏操作。截图位于 `debug/share-strm/export-mobile.png`。
- 执行 `npm run build` 验证前端生产构建。Docker Nginx 的 `/share-strm/` 应代理到 8082 后端，不能落入 SPA 静态页；外部代理参考 `docker/nginx.conf`，本地开发参考 `easy-strm-front/vite.config.js`。
- 上述测试不使用真实115凭据、不转存真实视频；数据库使用 sqlmock，任务使用 miniredis。真实115分享、Cookie有效性、CDN及媒体服务器网络连通性需在配置实际账号后验证。

2026-09-13 补充分享播放记录回归：控制器测试覆盖 GET/HEAD 成功 302 后记录、失败不记录；Service/DAO 测试覆盖导出元数据快照、分享映射标题与海报读取及 TMDB 相对海报路径补全。验证命令为后端 `go test ./...` 与前端 `npm run build`。

同日补充普通 STRM 海报回归：识别缓存构建测试防止再次漏存 `poster_path`，DAO 测试覆盖旧空海报按 TMDB ID 从本地缓存回查；`migrate_v35_playback_poster.sql` 幂等回填可确定归属的历史识别缓存和播放记录，同名多海报时不自动选择。

## 混合分享与AI识别回归（2026-09-08，维护者：Codex）

后端运行 `go test ./...`，覆盖混合类型、原盘命名、候选误匹配拒绝、AI协议和调用场景、配置接口、候选路径幂等及导入默认auto。前端启动Vite后运行 `node scripts/e2e-ai-recognition.mjs`、`node scripts/e2e-share-gallery.mjs`、`node scripts/e2e-share-clear.mjs` 和 `npm run build`。浏览器及AI协议测试使用本地模拟接口，不代表真实供应商可用性；用户可在AI配置页使用测试按钮验证自己的端点。

- 更新日期：2026-08-30
- 维护者：Codex

## 1. 当前开发基线

项目采用 Go 后端与 Vue 3 前端：

- 后端：Gin、PostgreSQL、Redis。
- 前端：Vue 3、Vite、Naive UI、Tailwind CSS。
- 自动化：Go test、Go vet、Vite build、Playwright 脚本。

修改代码时应保持 Controller、Service、DAO、Domain 的依赖方向；页面统一通过 `src/utils/api` 调用后端。

仪表盘账号容量使用 `easy_strm:dashboard:account_quota:<账号ID>` Redis 键，固定 TTL 为五分钟。Service 必须先读缓存，未命中、过期或损坏时再调用115接口并回填；Redis 故障只能降级为实时查询，不能阻断仪表盘。

## 2. 默认验证命令

后端：

```powershell
Set-Location .\easy-strm
gofmt -w <本次修改的 Go 文件>
go test ./...
go vet ./...
```

前端：

```powershell
Set-Location .\easy-strm-front
npm run build
```

媒体整理关键交互：

```powershell
Set-Location .\easy-strm-front
npm run e2e:organize-preview-refresh
npm run e2e:organize-preview-cancel
npm run e2e:emby-monitor
```

运行 E2E 前需要准备可登录的后端、前端服务和隔离测试数据。

Emby 监控专项 E2E 使用页面路由 Mock，不读写真实 Emby；覆盖六个页签、降级数据、海报兜底和 390px 窄屏布局。监控后端测试重点覆盖自然时间边界、插件响应归一化、排行/热力聚合、采样增量上限、暂停状态和 DAO 幂等更新。

## 3. 2026-07-23 功能收敛

本轮删除：

- `MediaLibrary.vue`、`SyncTasks.vue`、`PendingMedia.vue` 页面及路由菜单。
- 媒体同步、同步索引、入库流水线、待处理相关 API。
- 对应 Controller、Service、DAO、Domain、迁移和 Go 测试。
- 任务步骤持久化与任务详情步骤时间线。
- 115 生活事件同步实现及测试。
- 系统设置中的 Alist 地址、令牌表单及保存逻辑。
- 资源平台专用 E2E 和过期的媒体库规划文档。

兼容策略：应用不会主动 DROP 历史数据库中的 `t_media_sync_index`、`t_task_step`、`t_pending_media_item`，但新代码不会创建、读取或写入这些表。系统设置接口会隐藏并拒绝更新历史 `alist_url`、`alist_token` 配置键。

本轮验证结果：

| 验证项 | 结果 |
| --- | --- |
| `go test ./...` | 通过 |
| `go vet ./...` | 通过 |
| `npm run build` | 通过 |
| E2E 脚本语法检查 | 通过 |
| 下线功能残留检索 | 通过 |

## 4. 2026-08-05 115 云下载

本轮新增 115 云下载（离线下载）能力，支持一次提交多条 ed2k、magnet、http/https、ftp 链接。

2026-08-18 更新：超过 100 条的输入不再由前后端拒绝。入口校验账号和链接后立即创建任务中心任务，后端单工作队列按 `100 / 100 / ... / 余数` 顺序调用 115；100 条及以下继续沿用同步提交与逐链接受理结果。回归测试 `TestOfflineDownloadSubmitLargeBatchQueuesAndChunks` 使用 201 条链接验证入口不会等待115调用，并验证实际批次为 `100 / 100 / 1`。

变更范围：

- 后端：新增 `OfflineDownloadController`、`OfflineDownloadService`、`OfflineDownloadTaskDAO` 与 `domain.OfflineDownloadTask`，路由在 `auth.go` 统一注册；`115_offline.go` 封装 115driver 的离线下载增删查接口。
- 接口：
  - `POST /v1/resource/115-offline/submit`：提交链接（`cloud115_id`、`urls`、可选 `target_dir`），返回任务中心任务 ID 与逐链接受理结果。
  - `GET /v1/resource/115-offline/tasks`：按账号、状态分页查询记录。
  - `DELETE /v1/resource/115-offline/tasks/:id`：删除记录，`delete_files=true` 时同时删除 115 侧任务与已下载文件。
- 数据库：迁移 `migrate_v15_offline_download_task.sql` 创建 `t_offline_download_task`。
- 前端：新增 `src/views/resources/OfflineDownload.vue` 与 `src/utils/api/offline.js`，作为资源聚合页第二页签接入；`TaskCard.vue` 支持 `offline_download` 任务类型展示。

关键实现约束（回归时重点关注）：

- 115 批量离线接口遇到重复/无效链接时会整体拒绝，且响应错误码字段为 `errcode` 而非 `errno`，115driver 无法映射为哨兵错误，错误体为加密串。Service 层通过 `isOfflineDuplicateErr`、`isOfflineInvalidLinkErr`、`isOfflineQuotaErr` 做字符串识别，命中重复/无效时降级为逐链接提交，配额/登录类致命错误直接归一化返回。
- 链接已存在离线任务时按 URL 精确匹配、再按提取的 hash 兜底关联现有任务，不重复提交。
- 相关单测必须使用与生产响应形态一致的错误样本（含 `"errcode":10008` 加密体），不能只测哨兵错误。

本轮验证结果：

| 验证项 | 结果 |
| --- | --- |
| `go test ./...` | 通过（含云下载 service、controller、DAO 测试） |
| `go vet ./...` | 通过 |
| `gofmt` 检查 | 通过 |
| `npm run build` | 通过 |
| 真实链接联调 | 通过 |

真实联调证据（2026-08-05）：使用 `ed2k://|file|SSIS-472-C.mkv|10839656214|341A8F63D4058D871F66051533A080CA|/` 向「115主号」提交。该链接在账号中已有完成的离线任务，接口按降级路径关联现有任务：返回 `accepted=1` 与任务中心任务 ID，记录状态推进至 `completed`（percent=100，name=SSIS-472-C.mkv，size=10839656214），任务中心生成 `task_type=offline_download` 的已完成任务（success_files=1），证明提交、关联、跟踪、终态回写在真实 115 API 下全链路可用。

## 5. 测试覆盖重点

### 后端

- 媒体源创建、更新、浏览和路径处理。
- 本地与 115 文件操作。
- TMDB 解析、搜索、识别和元数据补全。
- 文件名规则默认模板、自定义规则热更新、正则与命名捕获组校验、示例匹配、S00 特别篇和季集边界。
- 重命名模板、分类匹配、整理预览和执行。
- 刮削输出与失败回退。
- STRM 配置和生成任务。
- Watch 自动整理、失败分类与重试。
- 任务取消、恢复和统一列表。
- Emby、缓存与直链接口。
- 115 云下载：链接归一化、批量提交、重复/无效链接降级、已存在任务关联、状态同步与记录删除。

### 前端

- 登录和路由守卫。
- 媒体源切换、文件浏览和批量选择。
- 自动识别、候选选择和手动修正。
- 识别测试页的本地解析、剧集季集展示、TMDB 失败提示、规则启停/排序/保存/恢复与窄屏操作。
- 重命名预览、整理预览刷新和取消。
- 整理结果、失败项重试、STRM 与 Emby 后续动作。
- 系统设置、日志、网络和缓存页面构建。
- 资源聚合页 115 云下载提交、记录列表轮询与删除交互。

## 6. 外部依赖验证

以下能力需要真实账号或服务，单元测试和构建通过不能替代真实联调：

- 115 登录、文件操作、秒传和直链。
- 115 离线下载（云下载）提交、状态查询与任务删除。
- TMDB 搜索、详情和图片下载。
- Emby 连接、媒体库列表和刷新。
- Telegram、企业微信应用、Server 酱和 SMTP 通知。

### Telegram 机器人

- 更新日期：2026-08-29
- 维护者：Codex
- 配置接口：`GET/PUT /notify/telegram/config`；读取结果只返回 `has_bot_token`，空 Token 更新会保留已有值。
- 运行接口：`GET /notify/telegram/status`、`POST /notify/telegram/test`。
- 长轮询命令：`/status`、`/tasks`、`/cron`、`/emby`、`/help`；仅配置的私聊 Chat ID 会被处理。
- 本地测试使用模拟 Telegram API、sqlmock 和 Redis 去重测试；真实 Bot、Chat ID 和外网代理仍需在部署环境完成连接测试。

### 企业微信应用通知

- 更新日期：2026-08-30
- 维护者：Codex
- 配置接口：`GET/PUT /notify/wecom/config`；读取结果只返回 `has_secret`，更新时 Secret 留空会保留已有值。
- 测试接口：`POST /notify/wecom/test`，使用已保存配置调用企业微信自建应用的 `gettoken` 与 `message/send` 官方接口。
- 配置至少包含 Corp ID、正整数 Agent ID、应用 Secret，以及成员、部门或标签中的一项接收范围；多个值使用 `|` 分隔。
- 统一事件监控覆盖 Telegram、企业微信、Server 酱和 SMTP；每个任务按渠道分别发送一次“任务已触发”和一次完成/失败/取消终态通知。Telegram 与企业微信的事件开关独立控制，Server 酱和 SMTP 对任务事件默认全开；Redis 基线、触发/终态去重和账号快照按渠道隔离，新启用或升级到新基线的渠道只建立基线，不补发历史任务。
- 本地测试通过模拟企业微信 API 覆盖鉴权参数、Markdown 发送字段、Token 缓存、业务错误、HTTP 错误和消息长度；真实企业和应用凭据需在部署环境通过页面测试按钮验证。
- API 接收接口：公开 `GET/POST /notify/wecom/callback`，经前端/Nginx 暴露为 `/api/notify/wecom/callback`。GET 用于企业微信后台 URL 校验，POST 接收加密 XML 并立即返回 `success`。
- 接收配置增加 `receive_enabled`、回调 Token 与 43 位 EncodingAESKey；读取接口只返回是否已配置，空值更新保留原值，兼容配置接口也必须删除三个敏感字段。
- 文本命令支持“帮助”“状态”“任务”及对应英文命令，并复用 Telegram 的 115 分享转存和云下载解析服务；Redis 对 MsgId 或消息摘要去重，避免平台重试导致重复任务。
- 企业微信后台至少选择“用户发送的普通消息”；回调 URL 必须是企业微信服务器可访问且符合企业要求的公网 HTTPS 域名，项目不会自动提供域名、证书或内网穿透。

验证外部能力时应使用隔离目录和测试账号，记录预期失败与真实缺陷的区别。

## 7. 高风险回归点

- 弹窗关闭、重新预览和再次执行时是否停止旧轮询。
- 本地与 115 路径、CID、pickcode 的语义是否混用。
- 冲突策略在预览和执行阶段是否一致。
- 复制模式不得删除源文件。
- Watch 任务是否有终态、取消和失败原因。
- STRM 内容、路径映射和直链 User-Agent 是否正确。
- 115 云下载记录列表轮询是否在无活跃记录时停止；删除记录时 115 侧任务与文件删除选项是否按预期生效。

## 8. 文档维护

- 产品范围、页面和架构变更更新 `docs/产品与架构.md`。
- 测试命令、回归结论和开发约束更新本文档。
- 一次性截图与调试证据放入 `debug/`。
- 不再新增按日期散落的需求路线图或测试报告。

## 9. 文件名识别规则开发约定

- 规则配置以 `filename_recognition_rules` 配置键存储在 `t_system_config`，不新增独立配置表；未配置或历史配置无效时使用代码内置模板。
- 正则针对标准化文件名执行：先去除扩展名，将点号、下划线和连字符转换为空格，再合并连续空白。
- 所有规则必须包含 `(?P<title>...)`；剧集规则必须包含 `(?P<episode>...)`，可选 `(?P<season>...)`，没有季捕获组时使用 `default_season`。
- 保存前必须覆盖正则编译失败、重复 ID、错误媒体类型、缺少捕获组、示例不匹配、全规则禁用等失败分支。
- 测试接口与整理服务必须统一调用 `TmdbService.ParseFilename`，不得在 Controller 或前端复制文件名业务规则。

## 10. 文件管理开发与测试约定

- 更新日期：2026-08-20
- 维护者：Codex

- 文件管理位置类型固定为 `local` 与 `cloud115`；本地位置 ID 是媒体源 ID，115 位置 ID 是账号 ID。
- 跨位置业务规则统一放在 `FileManagerService`，Controller 与 Vue 页面不得自行判断传输实现。
- 目录跨存储传输必须递归处理；跨边界剪切只有在整个顶层条目复制成功后才能删除源项。
- 115 下载必须携带 driver 返回的请求头；本地落盘先写临时文件，下载完整后再替换目标文件。
- 新增传输分支至少覆盖本地路径边界、目录递归、任务状态和对应115客户端调用。
- 默认回归命令为后端 `go test ./...`、`go vet ./...`、前端 `npm run build` 和 `git diff --check`。

## 11. Telegram 115 资源操作开发约定

- 更新日期：2026-08-30
- 维护者：Codex

- 文本解析、账号决策和任务提交集中在 `TelegramResourceService`，机器人生命周期服务只负责授权、异步调度和卡片回写。
- 分享链接优先于普通 HTTPS 云下载链接识别，避免115分享被误提交为离线下载。
- Telegram 与企业微信回调复用多行解析：以非空行为任务边界，每行独立应用 `链接 [账号名称] [/目标目录]` 语法并创建任务；批量执行必须在单行失败后继续，并汇总各行任务 ID 或失败原因。
- 多行回归测试至少覆盖全部成功、部分失败继续执行、逐行目录、企业微信纯文本和 Telegram HTML 汇总内容；单行命令格式必须保持兼容。
- 账号优先级数字越大越优先；同优先级必须以较小账号 ID 决胜，保证选择稳定。
- 分享密码使用标准 URL Query 解析，不能把空 Fragment 标记 `#` 计入密码。
- 新增语法或账号规则时至少覆盖链接、链接加目录、链接加账号、链接加账号和目录、默认账号排序、指定账号以及提交参数回传。

## 12. 媒体源监控开发与测试约定

- 更新日期：2026-08-30
- 维护者：Codex

- 媒体源只有在 `enabled=true` 且 `watch_enabled=true` 时才允许启动监控；前端状态文案和统计必须同时遵循这两个字段。
- 列表接口必须使用 WatchService 当前进程状态填充 `watch_running`；配置已开启但启动失败时显示“监控未运行”，不得显示为运行中。
- 新建媒体源未显式传递 `enabled` 时默认启用，编辑页面必须保留独立的启用/停用开关。
- 本地监控必须为已有子目录注册 fsnotify，并在新增目录进入时递归注册其目录树、发现其中视频文件。
- 115 监控必须完整分页并递归扫描监控目录；初始快照读取失败时不得以空快照启动，避免下次轮询误把历史文件当作新增文件。
- 回归测试至少覆盖创建默认启用、本地嵌套目录、115 子目录和超过单页上限的文件列表。

## 13. 网络代理开发与测试约定

- 更新日期：2026-08-30
- 维护者：Codex

- 配置有效的代理服务器地址后，Telegram、GitHub、TMDB 域名默认走代理，不依赖 `proxy_domains` 配置。
- `proxy_domains` 仅用于追加自定义站点，不得覆盖或关闭内置代理站点。
- 未配置代理服务器地址时所有请求保持直连；内置域名规则本身不得隐式启用代理。
- 网络探测和 TMDB、Telegram 等真实业务请求必须复用同一代理感知 HTTP 客户端规则。
- TMDB 网络探测必须动态读取已保存的 `tmdb_api_key` 并仅附加到实际请求；响应地址和错误信息不得包含明文 Key。
- 回归测试至少覆盖内置域名、非内置域名、自定义域名追加和业务客户端注入。

### 批量分享导入回归（2026-09-08，维护者：Codex）

- 后端：在 easy-strm 运行 `go test ./...`。share_import_test.go 使用虚构分享编号覆盖前置/后置/同行、Markdown、转义、裸访问码、缺省协议、域名去重、冲突、宣传过滤、块结束边界与无效输入；控制器覆盖JSON错误、空输入、无链接和真实解析响应，无数据库依赖。
- 前端：Vite 启动后，在 easy-strm-front 运行 `node scripts/e2e-share-batch-import.mjs`（E2E_FRONTEND_URL 默认 http://127.0.0.1:3001），随后运行 `npm run build`。
- 浏览器使用模拟API验证解析不写入、预览失效、冲突默认排除、名称/密码修改、选中项校验、部分成功后重试、列表刷新和390px窄屏按钮可见性。测试不写真实分享，不代表线上网盘有效性验证。


## 2026-09-09 验证记录（Codex）

- 后端：`go test ./...` 通过。
- 前端：`npm run build` 通过。
- 迁移：已增加分享资源库投影字段、索引、元数据补全所需数据，以及统一定时任务字段和执行记录表；未连接可用 PostgreSQL 实例，未执行线上迁移回放。
- 浏览器 E2E：未执行，当前环境没有可连接的完整后端服务；现有分享海报墙脚本未修改。


### API路由契约检查（更新：2026-09-10；维护者：Codex）
后端 `go test ./...` 包含 `TestFrontendAPIRoutes`：从 `auth.go` 和 `auth_routes_public.go` 提取实际注册路径与分组，核对前端 `api.*` / `request` 调用的HTTP方法和路径，避免仅在控制器单测中手动注册路由而遗漏正式装配。该测试使用空处理器，无数据库访问或业务写入；不替代真实部署的API集成验证。新增路由组或API调用形式时同步维护提取逻辑。
出现页面接口404时，先比较正式注册路径，再检查运行后端是否已重新构建/重启。Vite和Nginx将 `/api/` 前缀去掉后转发到8082，前端刷新不会加载新的Go路由。更新前后端时需同步发布。
