# 产品与架构

- 更新日期：2026-08-29
- 维护者：Codex

## 1. 产品定位

`easy-strm` 是面向个人媒体库的文件整理与 `.strm` 生成工作台。产品聚焦高频、可直接操作的媒体处理流程，不承担下载器管理或完整媒体资产治理平台的职责；资源获取仅覆盖 115 分享转存与 115 云下载（离线下载），不接入资源搜索与 PT。

核心流程：

```text
媒体源接入
  -> 文件浏览与选择
  -> TMDB 自动或手动识别
  -> 重命名与整理预览
  -> 移动、复制或链接
  -> 可选元数据刮削
  -> STRM 生成
  -> Emby 刷新与任务追踪
```

## 2. 功能范围

### 2.1 媒体源与文件

- 本地目录与 115 云盘媒体源。
- 媒体源启停、排序、监控目录、整理目标、媒体类型和冲突策略。
- 文件分页浏览、搜索、筛选、面包屑导航与批量选择。
- 本地和 115 文件移动、复制、删除、重命名；本地支持硬链接和软链接。

### 2.2 识别、命名与整理

- TMDB 电影、电视剧搜索和详情查询。
- 文件名解析、自动识别、候选选择、手动修正和缓存回流。
- 独立识别测试页可模拟真实整理解析，展示标题、媒体类型、年份、画质、来源、编码以及剧集季数和集数，并可继续查询 TMDB 候选。
- 文件名识别规则以有序模板配置，内置 `S01E05`、`1x05`、`Season 1 Episode 5`、`第1季第5集`、`EP05`、电影年份等常用格式；保存后立即应用于测试页和整理链路。
- 电影与电视剧命名模板。
- 整理候选扫描、异步预览、预览恢复、冲突策略和失败项重试。
- 按 Genre、国家/地区、语言、年份和标题关键字配置分类策略。

### 2.3 刮削与 STRM

- 生成电影、剧集 NFO。
- 下载海报、背景图和剧照。
- STRM 配置增删改查、全量生成、任务进度和定时执行。
- 通过公开 `/direct-link` 接口解析 115 播放直链。
- 整理完成后可生成 STRM 并触发 Emby 刷新。

### 2.4 自动化与运维

- 本地目录实时监控、115 目录轮询与自动整理。
- 自动整理失败分类、取消和重试。
- 统一任务列表、详情、进度、结果与失败信息。
- Dashboard、日志、网络检测、Redis 缓存管理和系统配置。
- Telegram、Server 酱和 SMTP 通知后端。Telegram 支持单管理员私聊长轮询机器人，可查询系统概览、最近任务、定时任务和 Emby 状态，并直接执行任务取消/自动整理重试、定时任务运行和 Emby 刷新。
- Telegram 任务终态与 115 账号状态卡片使用 HTML 富文本和内联按钮；Redis 负责首次启用基线、任务事件和账号快照去重，通知失败不影响业务任务。
- Docker Compose 部署 PostgreSQL、Redis 和应用服务。

### 2.5 资源聚合与 115 云下载

资源聚合页承载 115 生态的资源获取能力，包含两个页签：

- **115 分享转存**：分享链接一键解析与批量转存。
- **115 云下载**（离线下载）：
  - 支持一次提交多条 ed2k、magnet、http/https、ftp 链接，自动去重、去空白并过滤不支持的协议。
  - 可选择 115 账号与保存目录，未指定目录时默认使用 `/云下载`。
  - 100 条及以下链接同步提交；超过 100 条时立即返回本地任务 ID，由后端单工作队列按每批最多 100 条顺序发送到 115，并在任务中心持续更新提交与下载状态。
  - 批量提交被 115 以「任务已存在/链接无效」整体拒绝时，自动降级为逐链接提交，精确标注每条链接的受理结果。
  - 链接已存在离线任务时不重复提交，而是关联现有任务并继续跟踪其状态。
  - 提交成功返回任务中心任务 ID，云下载任务可在任务中心查看进度与结果。
  - 后台跟踪协程轮询 115 离线任务状态，把记录推进到完成、失败或已移除等终态；115 侧配额不足、登录失效等错误归一化为可读提示。
  - 云下载记录支持按账号、状态分页查询，删除记录时可选择是否同时删除 115 侧任务与已下载文件。

## 3. 已下线范围

2026-07-23 起，下列低频功能不再属于产品范围：

- 媒体资产台账。
- 媒体源全量/增量同步及同步索引。
- 入库流水线。
- 识别失败待处理清单。
- 任务持久化步骤时间线。
- 115 生活事件增量同步。
- 系统设置中的全局 Alist 地址和令牌配置。

代码不再注册相关页面或接口，也不再为新环境创建对应数据库表。历史数据库中的旧表和旧配置行不会由应用启动过程自动删除。

## 4. 页面结构

### 资源整理

- 仪表盘
- 任务中心

### 支撑配置

- 文件工作台
- STRM 配置
- 115 云管理
- 整理规则
- 识别测试（文件名解析、TMDB 候选与识别规则配置）
- 系统设置
- 资源聚合（115 分享转存、115 云下载）

### 运维观察

- 系统日志
- 网络测试
- 缓存管理

## 5. 技术架构

| 层级 | 技术与职责 |
| --- | --- |
| 前端 | Vue 3、Vite、Naive UI、Tailwind CSS；页面与交互编排 |
| Controller | Gin HTTP 参数、响应和路由适配 |
| Service | 识别、整理、刮削、STRM、监控、资源转存与云下载、任务业务流程、Telegram 机器人与通知事件 |
| DAO | PostgreSQL、Redis 持久化访问 |
| 外部适配 | 115、TMDB、Emby、通知和本地文件系统 |

依赖方向：

```text
Vue 页面 -> utils/api -> Controller -> Service -> DAO/外部适配
```

## 6. 当前边界与限制

- STRM 增量生成接口尚未实现，应使用全量生成。
- 115 开放平台登录等待开发者账号，当前主要使用 Cookie 扫码登录。
- 真实 TMDB、115、Emby 和通知成功链路依赖用户环境配置。
- Telegram 首版仅支持一个管理员私聊 Chat ID，不支持群组、多用户白名单、Webhook 或服务重启等高影响操作。
- 当前为单工作台登录模式，不提供多用户角色权限。
- 不接入 PT、下载器、资源搜索、订阅和插件市场。

## 7. 演进原则

后续优先稳定文件工作台主链路，减少并行产品面。新增能力必须直接服务于“浏览、识别、整理、刮削、STRM、追踪、资源获取（115 转存与云下载）”之一，并复用现有 Service、API 和 UI 体系。

## 8. 统一文件管理

- 更新日期：2026-08-20
- 维护者：Codex

文件管理页位于 `/dashboard/file-manager`，采用双栏布局。每一栏都可以直接选择一个本地媒体源或一个 115 账号、独立浏览目录并选择多个文件；复制和剪切形成页级剪贴板，随后可在任意一栏的当前目录执行粘贴。

跨位置传输由后端 `FileManagerService` 统一编排并写入任务中心：

- 本地到本地：递归复制文件系统条目。
- 本地到 115：递归创建115目录并通过 115driver 上传文件。
- 115 到本地：递归读取115目录，透传下载签名请求头并以临时文件完成本地落盘。
- 同账号 115 到 115：使用115原生复制或移动。
- 不同账号 115 到 115：目标账号递归建目录，文件逐个使用跨账号秒传。

剪切跨越存储或账号边界时始终先完成复制，再删除对应源项；失败任务保留源数据并在任务中心记录错误。本地位置只允许访问配置媒体源根目录之内的路径。
